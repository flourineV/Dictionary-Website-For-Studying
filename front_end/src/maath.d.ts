declare module "maath/random/dist/maath-random.esm" {
  export function inSphere(
    array: Float32Array,
    options?: { radius?: number }
  ): Float32Array;
  // Khai báo các hàm khác của maath mà bạn sử dụng nếu cần
}
